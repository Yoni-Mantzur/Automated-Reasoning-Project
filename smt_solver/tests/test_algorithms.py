from smt_solver.formula import Formula

#
# def test_find_representative():
#     f = Formula.from_str('f(f(a))=b')
#     terms = f.terms.values()
#     representative = terms.nex
#     for term in terms:
#         term.next_ptr =